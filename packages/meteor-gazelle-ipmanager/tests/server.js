Tinytest.add('IpManager - banIpAddress', function (test) {
  /*
  // Arrange
  BannedIp.remove({});
  var ipAddressToBan = '127.0.0.1';
  var notes = 'Test notes';

  // Act
  Meteor.call('banIpAddress', ipAddressToBan, notes);

  // Assert
  var bannedIpAddress = BannedIp.findOne({startIpStr: ipAddressToBan});
  test.isNotUndefined(bannedIpAddress);

  BannedIp.remove({});
  */
});

// TODO(rhomes) moar tests
