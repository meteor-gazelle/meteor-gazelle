/*
import { Permissions } from '../permissions.js';

export const ManagePermissions = React.createClass({
  mixins: [ReactMeteorData],

  propTypes: {
    thingId: React.PropTypes.string,
    thingType: React.PropTypes.oneOf(['user', 'class']).isRequired,
  },

  getMeteorData() {
    let data = {};
    let subscriptionName;

    if (thingId) {
      subscriptionName = thingType === 'user' ? 'userPermissions' : ''
    }

    if (thingId) {
      if (thingType === 'user') {

      } else if (thingType === 'class') {

      }
    }

    return data;
  },


});
*/
