//TODO(ajax) Investigate why this is causing collection2 to throw an error
/*
const userPermissionsSchema = new SimpleSchema({
  permissionsEnabled: {
    type: [String],
    optional: true,
    index: true
  },
  permissionsDisabled: {
    type: [String],
    optional: true,
    index: true
  }
});

Meteor.users.attachSchema(userPermissionsSchema);
*/
