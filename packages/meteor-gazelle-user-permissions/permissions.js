Permissions = {};

// 1. Register roles
//  a. Roles map to alaaning roles
// 2. Check if user has role
// 3. Expose Permissions name
// 4. Server side meteor methods for check

Permissions.registerRoles = function(name, roles) {
  
};
